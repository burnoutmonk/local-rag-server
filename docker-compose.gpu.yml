services:
  model_downloader:
    build:
      context: .
      dockerfile: Dockerfile.cuda

  ingest:
    build:
      context: .
      dockerfile: Dockerfile.cuda

  llm:
    build:
      context: .
      dockerfile: Dockerfile.cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  api:
    build:
      context: .
      dockerfile: Dockerfile.cuda

  benchmark:
    build:
      context: .
      dockerfile: Dockerfile.cuda